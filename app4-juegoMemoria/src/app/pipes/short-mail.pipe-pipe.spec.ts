import { ShortMailPipePipe } from './short-mail.pipe-pipe';

describe('ShortMailPipePipe', () => {
  it('create an instance', () => {
    const pipe = new ShortMailPipePipe();
    expect(pipe).toBeTruthy();
  });
});
