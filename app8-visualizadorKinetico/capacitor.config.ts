import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'app8-visualizadorKinetico',
  webDir: 'www'
};

export default config;
